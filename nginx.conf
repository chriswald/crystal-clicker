server {
	listen 0.0.0.0:80;

	root /var/www/html;
	index index.html;

	location / {
		try_files $uri.html $uri $uri/ @rewrite;
	}

	gzip on;
	gzip_types application/javascript image/* text/css text/plain;
	gunzip on;
}